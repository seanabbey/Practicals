BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'horizontal'
        TextInput:
            id: input_m
            text: '34'
            on_text: app.handle_convert(int(input_m.text))
        BoxLayout:
            orientation: 'vertical'
            Button:
                text: 'Up'
                on_press: app.handle_increment(1)
            Button:
                text: 'Down'
                on_press: app.handle_increment(-1)

#    Button:
#        text: 'Convert miles to km'
#        on_press: app.handle_convert(int(input_m.text))

    Label:
        id: output_label
        text: '54.717'
